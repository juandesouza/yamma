# Environment (use *.example templates; never commit real secrets)
backend/.env
backend/.env.local
web/.env
web/.env.local
mobile/.env
mobile/.env.local

node_modules
dist
.next
*.log
*.tsbuildinfo
.DS_Store
coverage
.turbo

# Local tooling binary (e.g. nhost CLI) — do not commit
/cli

.vercel
