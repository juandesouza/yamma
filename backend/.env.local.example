# Optional — local Postgres only (overrides `DATABASE_URL` in `.env` when present):
#   docker compose up -d   # from repo root
#   cp .env.local.example .env.local
# Remove or rename `.env.local` when you want the app to use nHost only.
#
DATABASE_URL=postgresql://postgres:yamma_local_dev@localhost:5432/yamma
